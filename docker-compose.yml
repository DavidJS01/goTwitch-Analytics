version: '3.8'
services:
  db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
    volumes: 
      - db:/var/lib/postgresql/data
volumes:
  db:
    driver: local

    # docker run --name postgres-db -e POSTGRES_PASSWORD=$DOCKER_POSTGRES_PASSWORD -p 5432:5432 -d postgres
